if [ "`uname`" = "Darwin" ]; then
  alias ls='ls -G'
fi

if [ "`uname`" = "Linux" ]; then
  alias ls='ls --color'
fi

alias quit='tmux kill-session'

[[ -s "$HOME/.aliases.local" ]] && source ~/.aliases.local
